/**
 * <p> FACADE - package implements business logic.
 * FACADE classes will contain not just the action needed to perform action in the Coupon system application (DAO and DAODB) but also the testing needed
 * for example CustomerFacade would contain first a check of any Coupons left and only then purchaseCoupon(Coupon).
 * All Facade classes implements CouponClientFacade interface
 * </p>
 * <img src="{@docRoot}/images/facade.jpg" alt="facade" style="width:850px">
 */
package g_coupon.sys.core.facade;